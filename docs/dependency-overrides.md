# pnpm.overrides provenance

The root `package.json` pins transitive dependency versions via `pnpm.overrides`
because upstream parents (firebase-admin, firebase-tools, @google-cloud/storage,
gaxios, google-auth-library, and various dev/build tooling) had not yet picked up
patched versions of these packages when a Dependabot advisory was opened against
them. JSON does not support comments, so provenance is tracked here instead.

When bumping an override, verify whether the parent package now ships a version
that includes the fix; if so, remove the override entirely rather than bumping it.

| Package             | Why                                                                                                                                                                                                                                                                                              | Date / advisory                                    | Removal condition                                                                                                           |
| ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| `uuid`              | Missing buffer bounds check in v3/v5/v6 when `buf` is provided (uuid <14.0.0). Transitive via firebase-admin, @google-cloud/storage, gaxios, google-auth-library, firebase-tools.                                                                                                                | 2026-04-29, Dependabot alert #2 (PR #1)            | Remove once all listed parents depend on uuid ^14 natively.                                                                 |
| `@tootallnate/once` | Incorrect control-flow scoping CVE in `@tootallnate/once` <3.0.1. Transitive via http-proxy-agent@5.0.0 → teeny-request → @google-cloud/storage.                                                                                                                                                 | 2026-04-29, Dependabot alert #1 (PR #1)            | Remove once @google-cloud/storage's teeny-request chain uses http-proxy-agent depending on @tootallnate/once ^3.0.1+.       |
| `protobufjs`        | Dependabot advisory patch floor pinned as part of a batch of 22 dev/build-tooling advisories (commit `7abf59d`). **Reaches production**: resolves via `google-gax` under `firebase-admin`/`firebase-functions`, both production dependencies of `apps/functions`.                                | 2026-05-21                                         | Remove once `firebase-admin`/`firebase-functions` (via `google-gax`) bump past ^7.5.8 natively.                             |
| `@protobufjs/utf8`  | Same batch as `protobufjs` above (commit `7abf59d`). **Reaches production** via the same `google-gax` → `firebase-admin`/`firebase-functions` chain (`apps/functions` production deps).                                                                                                          | 2026-05-21                                         | Remove once the parent depending on this sub-package bumps past ^1.1.1 natively.                                            |
| `hono`              | Same batch (commit `7abf59d`). Note: `hono` is also a direct production dependency of `apps/pdf-renderer`; confirm the direct dependency version and this override stay in sync before removing.                                                                                                 | 2026-05-21                                         | Remove once no transitive consumer resolves to a pre-4.12.18 hono, or once the direct dependency alone satisfies the floor. |
| `ws@8`              | Same batch (commit `7abf59d`). Pinned specifically to the `ws` 8.x line (`ws@8` selector) so any `ws` 6.x/7.x transitive deps are left alone. **Reaches production**: resolves via `puppeteer-core` under `puppeteer`, a production dependency of `apps/pdf-renderer` (used at runtime for CDP). | 2026-05-21                                         | Remove once `puppeteer`/`puppeteer-core`'s `ws` 8.x dependency resolves to ^8.20.1+ natively.                               |
| `basic-ftp`         | Same batch (commit `7abf59d`). Dev-only.                                                                                                                                                                                                                                                         | 2026-05-21                                         | Remove once the parent depending on basic-ftp bumps past ^5.3.1 natively.                                                   |
| `fast-uri`          | Same batch (commit `7abf59d`). Dev-only.                                                                                                                                                                                                                                                         | 2026-05-21                                         | Remove once the parent depending on fast-uri bumps past ^3.1.2 natively.                                                    |
| `fast-xml-builder`  | Same batch (commit `7abf59d`). **Reaches production**: resolves via `fast-xml-parser` under `@google-cloud/storage`, pulled in by `firebase-admin`, a production dependency of `apps/functions`.                                                                                                 | 2026-05-21                                         | Remove once `firebase-admin`'s `@google-cloud/storage`/`fast-xml-parser` dependency bumps past ^1.1.7 natively.             |
| `ip-address`        | Same batch (commit `7abf59d`). Dev-only.                                                                                                                                                                                                                                                         | 2026-05-21                                         | Remove once the parent depending on ip-address bumps past ^10.1.1 natively.                                                 |
| `brace-expansion@5` | Same batch (commit `7abf59d`). Pinned specifically to the `brace-expansion` 5.x line so the 1.x/2.x lines used elsewhere in the tree are left untouched. Dev-only.                                                                                                                               | 2026-05-21                                         | Remove once all transitive consumers of `brace-expansion` 5.x resolve to ^5.0.6+ natively.                                  |
| `tmp`               | Path traversal advisory, dev-only.                                                                                                                                                                                                                                                               | 2026-06-08, Dependabot advisory (commit `6d8c8ec`) | Remove once the parent depending on tmp bumps past ^0.2.6 natively.                                                         |
| `qs`                | `qs.stringify` DoS advisory. **Reaches production**: resolves via `googleapis-common` under `googleapis`, a production dependency of `apps/functions`.                                                                                                                                           | 2026-06-08, Dependabot advisory (commit `6d8c8ec`) | Remove once `googleapis`'s `googleapis-common` dependency bumps its `qs` floor past ^6.15.2 natively.                       |

To find the current parent(s) still pulling in a pre-fix version of any package
above, run `pnpm why <package>` from the repo root and check whether every path
already resolves at or above the pinned floor without the override.
